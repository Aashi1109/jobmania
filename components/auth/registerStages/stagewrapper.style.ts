import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    gap: 16,
  },
  buttonContainer: {
    gap: 8,
    flexDirection: "row",
  },
});

export default styles;
